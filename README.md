# task-manager
node-js project of a simple task manager that does basic CRUD operations

Task Manager project that takes 'task-name' and completed(boolean) as inputs
Database used is mongodb with mongoose driver

Operations  : GetAllTasks, CreateTask, DeleteTask, UpdateTask, GetTaskById

